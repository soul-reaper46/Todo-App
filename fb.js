import * as firebase from 'firebase'


 var firebaseConfig = {
    
    //put ur firebase api credentials here

  };
  // Initialize Firebase
  firebase.initializeApp(firebaseConfig);

  export default firebase